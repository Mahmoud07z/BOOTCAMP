// Transform winBattle to an arrow function
const winBattle = () => true;

// Use a ternary operator to assign experiencePoints
const experiencePoints = winBattle() ? 10 : 1;

// Log the result
console.log(experiencePoints);